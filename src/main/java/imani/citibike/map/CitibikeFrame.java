package imani.citibike.map;

import javax.swing.*;

public class CitibikeFrame extends JFrame {

    public CitibikeFrame() {
        setTitle("Citibike Map");
        setSize(800, 600);
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);

        //getContentPane().add(mapViewer);
    }

    /*
        what i need to do in this class:
        i want the jmap on the center
        then on the bottom buttons
     */


}
